const assert = require("assert");
const { Given, When, Then } = require("@cucumber/cucumber");

Given("Un usuario quiere escribir una reseña a un profesor", function () {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

When("El usuario escribe la reseña", function () {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then(
  "El sistema verifica que la reseña no exceda los {int} caracteres",
  function (int) {
    // Then('El sistema verifica que la reseña no exceda los {float} caracteres', function (float) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);

When("El usuario escribe la reseña que sobrepasa el límite", function () {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

Then(
  "El sistema verifica que la reseña excede los {int} caracteres y avisa al usuario",
  function (int) {
    // Then('El sistema verifica que la reseña excede los {float} caracteres y avisa al usuario', function (float) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);

Then(
  "El sistema verifica que la reseña no exceda los {int} caracteres y avisa al usuario que debe ser más conciso",
  function (int) {
    // Then('El sistema verifica que la reseña no exceda los {float} caracteres y avisa al usuario que debe ser más conciso', function (float) {
    // Write code here that turns the phrase above into concrete actions
    return "pending";
  }
);

Given("Un usuario quiere editar una reseña a un profesor", function () {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});

When("El usuario edita la reseña", function () {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});
